{
  "name": "saloon-managements-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "migrate:cloud": "cd server && npm install && npm run migrate:cloud"
  }
}
